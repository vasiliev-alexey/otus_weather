import {
  renderInitialData,
  renderSearchForm,
  renderCityList,
  // eslint-disable-next-line import/extensions
} from './js/renderData.js';

renderInitialData();

renderSearchForm();

renderCityList();
